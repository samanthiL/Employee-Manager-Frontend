// src/setupTests.ts

import '@testing-library/jest-dom'; // Optional: Adds custom matchers to Jest (like `toBeInTheDocument`)
import { expect } from '@jest/globals'; // Optional: If you want to customize or extend Jest's global expectations

// Any other global setups like mocking libraries or modules
